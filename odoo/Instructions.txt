1 - Install Docker and docker-compose on your machine

2 - download the project files to your machine

3 - open the project directory on the terminal

4 - Run the following command:

	> docker-compose up -d --build